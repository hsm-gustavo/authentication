PORT=8080
# se estiver rodando em dev
DATABASE_URL=postgresql://postgres:senha@localhost:5432/auth?sslmode=disable
# se estiver rodando em produção
# DATABASE_URL=postgresql://postgres:senha@db:5432/auth?sslmode=disable
ENV=development
JWT_SECRET=sua-chave-secreta